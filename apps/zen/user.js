// Zen browser: allow chrome/userChrome.css and chrome/userContent.css to load.
// Append to <profile>/user.js (create it if missing) and restart Zen.
user_pref("toolkit.legacyUserProfileCustomizations.stylesheets", true);
